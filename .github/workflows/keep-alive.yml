name: Keep-alive ping

on:
  schedule:
    - cron: '0 6 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API to keep Supabase project active
        run: curl -fsS https://my-blogpost-hhy6.onrender.com/api/posts
